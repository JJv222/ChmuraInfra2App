apiVersion: 1

providers:
  - name: 'Moje Dashboardy'
    orgId: 1
    folder: ''           # Pusty string = folder General. Możesz wpisać np. 'Backend'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards # To jest ścieżka WEWNĄTRZ kontenera