export const environment = {
  // Read API base URL from a global window variable if present, otherwise use default
  // To override at runtime, set window.API_BASE_URL = 'https://your-api.example.com/api'
  apiBaseUrl: '/api'
};
